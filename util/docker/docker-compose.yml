services:
  openldap:
    image: bitnami/openldap:latest
    container_name: ldap-util-openldap
    restart: no
    profiles:
      - default
      - ldap
    env_file: ldap.env
    ports:
      - "1389:1389"