authentication:
  domain:
    urls: "ldap://${ad.hostname}:${ad.http.port}"
    baseDn: "DC=example,DC=local"
    domain: "example.local"
  nodomain:
    urls: "ldap://${ad.hostname}:${ad.http.port}"
    baseDn: "DC=example,DC=local"
