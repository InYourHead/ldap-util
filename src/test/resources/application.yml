spring:
  profiles:
    default:
      - test
      - ldap
      - ad
  application:
    name: "ldap-util-test"
logging:
  level:
    org:
      springframework:
        security:
          ldap: DEBUG
          config:
            ldap: DEBUG
server:
  error:
    include-exception: true