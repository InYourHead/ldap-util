spring:
  profiles:
    default: test
  application:
    name: "ldap-util-test"
test:
  ldap:
    integration:
      cn:
        urls: "ldap://localhost:${ldap.http.port}"
        baseDn: "dc=example,dc=org"
      uid:
        urls: "ldap://localhost:${ldap.http.port}"
        baseDn: "dc=example,dc=org"
        userSearchFilter: "uidNumber={0}"

logging:
  level:
    org:
      springframework:
        security:
          ldap: DEBUG